# Sea Turtle Image Segmentation and Identification

This project provides a comprehensive exploration of image analysis techniques for sea turtle conservation research. It tackles two primary computer vision tasks: semantic segmentation to isolate turtle body parts (shell, head, limbs) and classification to identify these parts. The repository compares two distinct methodologies: a modern, end-to-end Deep Learning approach using state-of-the-art architectures like U-Net, DeepLabV3+, and PSPNet, and a traditional Computer Vision pipeline leveraging feature extraction techniques like SIFT and HOG coupled with classic Machine Learning classifiers such as SVM and Random Forest.

---

## 🚀 Live Demo & Key Features


### Key Features:
*   **Multiple Deep Learning Models:** Implemented and compared state-of-the-art architectures including **U-Net**, **DeepLabV3+**, and **PSPNet** for semantic segmentation.
*   **Classic CV/ML Pipeline:** A full pipeline demonstrating traditional techniques:
    *   Color-based segmentation with OpenCV.
    *   Feature extraction using **SIFT** and **HOG**.
    *   Training and comparison of multiple classifiers like **SVM**, **KNN**, and **Random Forest**.
*   **Reusable Evaluation Script:** A standalone `evaluation.py` script to easily evaluate any trained model on a test set.
*   **Large Model Handling:** All trained model weights are tracked using **Git LFS** for efficient repository management.

---

## 📂 Project Structure

```
.
├── trained_models/             # Contains trained model weights (.pth, .pkl) tracked by Git LFS
├── evaluation.py               # Standalone script to evaluate models
├── Part_Classification-Classic_ML.ipynb  # Notebook for the classic CV/ML approach
├── Segmentation-DeepLabV3+.ipynb # Notebook for DeepLabV3+ implementation
├── Segmentation-PSPNet.ipynb     # Notebook for PSPNet implementation
├── Segmentation-UNet.ipynb       # Notebook for U-Net implementation
├── .gitattributes              # Git LFS configuration file
├── README.md                   # This file
├── requirements.txt            # Project dependencies
└── .gitignore                  # Files to be ignored by Git
```

---

## 🛠️ Installation & Setup

To run this project, follow these steps. A virtual environment is recommended.

1.  **Clone the repository:**
    ```bash
    git clone [Your GitHub Repository URL]
    cd Sea-Turtle-Image-Segmentation-and-Identification
    ```

2.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Download the trained models:**
    This project uses Git LFS to handle large model files. Ensure Git LFS is installed. Then, pull the model files:
    ```bash
    git lfs pull
    ```

---

## 📖 Usage

### 1. Dataset
The dataset used for this project is [**Insert Dataset Name Here, e.g., the TU-Platypus dataset**]. You can download it from [**Insert Link to Your Dataset Source Here**].

Please download the data and structure it inside a `preprocessed_data` directory at the root of the project as follows:
```
preprocessed_data/
├── train/
│   ├── images/
│   └── masks/
├── valid/
│   ├── images/
│   └── masks/
└── test/
    ├── images/
    └── masks/
```

### 2. Exploring the Notebooks
You can explore the model implementations, training processes, and results by running the Jupyter Notebooks (`.ipynb` files) in the root directory.

### 3. Evaluating a Model
Use the `evaluation.py` script to evaluate the performance of the trained models on the test set.

**Example: Evaluate the U-Net segmentation model:**
```bash
python evaluation.py --model-path trained_models/best_unet_seaturtle.pth --data-path ./preprocessed_data --model-type unet
```

**Example: Evaluate the SVM classification model:**
*(Note: This assumes you have an extracted features folder named 'features_sift' inside 'preprocessed_data' as generated by the Classic_ML notebook)*```bash
python evaluation.py --model-path trained_models/hog_svm_classifier.pkl --data-path ./preprocessed_data --model-type svm
